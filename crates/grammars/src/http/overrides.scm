; Keep punctuation that is meaningful in HTTP values as part of an active
; completion query. The matching settings live in config.toml.
[
  (target_url)
  (header_entity)
  (value)
  (identifier)
  (variable_name)
  (variable_expression)
  (path)
] @http_completion
